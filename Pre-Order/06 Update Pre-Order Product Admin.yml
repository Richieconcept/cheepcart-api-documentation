info:
  name: 06 Update Pre-Order Product Admin
  type: http
  seq: 6

http:
  method: PUT
  url: "{{baseUrl}}/api/pre-order-products/69ecb429354aed94116d5bbf"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  body:
    type: multipart-form
    data:
      - name: name
        type: text
        value: Luxury Travel Bag Updated
      - name: price
        type: text
        value: "48000"
      - name: status
        type: text
        value: available
      - name: displayOrder
        type: text
        value: "2"
      - name: image
        type: file
        value: []
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  # Update Pre-Order Product

  ## Endpoint
  `PUT /api/pre-order-products/:id`

  ## Who uses this?
  Admin dashboard.

  ## Authentication
  Required. Admin only.

  ## What it does
  Updates an existing pre-order product.

  You only need to send the fields you want to change.
  If you send a new `image` file, the backend uploads it to Cloudinary and replaces the saved product image.

  ## URL parameter

  `id`
  - Type: string
  - Required: yes
  - This is the product `_id`.

  ## Body type
  `multipart/form-data`

  ## Example form data

  ```txt
  name: Luxury Travel Bag Updated
  price: 48000
  status: available
  displayOrder: 2
  ```

  To replace the product image, also send:

  ```txt
  image: [file]
  ```

  Important: `image` is optional during update. If the admin does not choose a new image, do not send the `image` field.

  ## Other fields you can update

  ```txt
  description: New description
  isActive: true
  ```

  ## Image replacement behavior

  - Send `image` as a file using the exact key `image`.
  - Allowed file type: image.
  - Max file size: 5MB.
  - Backend uploads the new file to Cloudinary.
  - Backend saves the returned `secure_url` and `public_id`.
  - Backend deletes the old Cloudinary image using the old `public_id`.

  ## Sample success response

  ```json
  {
    "message": "Pre-order product updated successfully",
    "product": {
      "_id": "662b7f8f4f9c2e2d7c111111",
      "name": "Luxury Travel Bag Updated",
      "slug": "luxury-travel-bag-updated",
      "price": 48000,
      "image": {
        "secure_url": "https://res.cloudinary.com/demo/image/upload/v1234567890/cheepcart/pre-order-products/new-bag.jpg",
        "public_id": "cheepcart/pre-order-products/new-bag"
      },
      "status": "available",
      "displayOrder": 2,
      "isActive": true
    }
  }
  ```

  ## Error response

  ```json
  {
    "message": "Pre-order product not found"
  }
  ```

  ## Frontend example

  ```js
  const formData = new FormData();
  formData.append("name", name);
  formData.append("price", price);
  formData.append("status", status);
  formData.append("displayOrder", displayOrder);

  if (imageFile) {
    formData.append("image", imageFile);
  }

  await fetch(`${baseUrl}/api/pre-order-products/${productId}`, {
    method: "PUT",
    headers: {
      Authorization: `Bearer ${adminToken}`
    },
    body: formData
  });
  ```

  Do not manually set `Content-Type` in frontend when using `FormData`; the browser will add the multipart boundary automatically.
