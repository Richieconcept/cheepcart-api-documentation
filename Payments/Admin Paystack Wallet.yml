info:
  name: Admin Paystack Wallet
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/payments/admin/paystack-wallet?perPage=20&page=1&status=success"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  params:
    - name: perPage
      value: "20"
      type: query
    - name: page
      value: "1"
      type: query
    - name: status
      value: success
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  # Admin Paystack Wallet

  `GET /api/payments/admin/paystack-wallet`

  ## Description

  Returns Paystack wallet balances, recent Paystack transactions, and transaction totals for the admin dashboard.

  The frontend should call this backend endpoint. Do not call Paystack directly from frontend because Paystack requires the secret key.

  ## Authentication

  Required. Admin only.

  ```http
  Authorization: Bearer <admin-token>
  ```

  ## Query Parameters

  | Name | Type | Required | Description |
  |------|------|----------|-------------|
  | `page` | number | No | Transaction page. Default: `1`. |
  | `perPage` | number | No | Transactions per page. Default: `20`, max: `100`. |
  | `status` | string | No | Paystack transaction status. Common values: `success`, `failed`, `abandoned`. |
  | `from` | string | No | Start date, for example `2026-05-01` or ISO datetime. |
  | `to` | string | No | End date, for example `2026-05-05` or ISO datetime. |
  | `currency` | string | No | Currency code, for example `NGN`. |
  | `customer` | number | No | Paystack customer ID filter. |

  ## Example Request

  ```http
  GET /api/payments/admin/paystack-wallet?perPage=20&page=1&status=success
  Authorization: Bearer <admin-token>
  ```

  ## Success Response

  ```json
  {
    "success": true,
    "wallet": {
      "balances": [
        {
          "currency": "NGN",
          "balanceMinor": 1700000,
          "balance": 17000,
          "formattedBalance": "NGN 17,000.00"
        }
      ],
      "primaryBalance": {
        "currency": "NGN",
        "balanceMinor": 1700000,
        "balance": 17000,
        "formattedBalance": "NGN 17,000.00"
      }
    },
    "transactions": {
      "page": 1,
      "perPage": 20,
      "meta": {
        "next": null,
        "previous": null,
        "perPage": 20
      },
      "data": [
        {
          "id": "4099260516",
          "reference": "PAY-CHP-20260330-0001-1711800000000",
          "status": "success",
          "gatewayResponse": "Successful",
          "amountMinor": 5055200,
          "amount": 50552,
          "formattedAmount": "NGN 50,552.00",
          "feesMinor": 75000,
          "fees": 750,
          "formattedFees": "NGN 750.00",
          "currency": "NGN",
          "channel": "card",
          "paidAt": "2026-05-05T10:30:00.000Z",
          "createdAt": "2026-05-05T10:28:00.000Z",
          "customer": {
            "email": "customer@example.com",
            "firstName": "Jane",
            "lastName": "Doe",
            "customerCode": "CUS_123"
          },
          "order": {
            "orderId": "69ca82d41d13ed0cbf279f25",
            "orderNumber": "CHP-20260330-0001"
          }
        }
      ]
    },
    "totals": {
      "totalTransactions": 24,
      "totalVolumeMinor": 120000000,
      "totalVolume": 1200000,
      "formattedTotalVolume": "NGN 1,200,000.00",
      "totalVolumeByCurrency": [
        {
          "currency": "NGN",
          "amountMinor": 120000000,
          "amount": 1200000,
          "formattedAmount": "NGN 1,200,000.00"
        }
      ],
      "pendingTransfersMinor": 0,
      "pendingTransfers": 0,
      "formattedPendingTransfers": "NGN 0.00",
      "pendingTransfersByCurrency": []
    }
  }
  ```

  ## Frontend Mapping

  - Wallet card: use `wallet.primaryBalance.formattedBalance`.
  - Balance by currency: map `wallet.balances`.
  - Recent payments table: map `transactions.data`.
  - Transaction amount: display `formattedAmount`.
  - Paystack fee: display `formattedFees`.
  - Customer email: use `customer.email`.
  - Order reference: use `order.orderNumber` when available.
  - Summary card: use `totals.formattedTotalVolume` and `totals.totalTransactions`.

  ## Error Response

  ```json
  {
    "success": false,
    "message": "Paystack error message"
  }
  ```
