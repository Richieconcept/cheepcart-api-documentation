info:
  name: 03 Update Category Admin
  type: http
  seq: 3

http:
  method: PUT
  url: "{{baseUrl}}/api/categories/662b7f8f4f9c2e2d7c111111"
  headers:
    - name: Authorization
      value: Bearer {{token}}
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "name": "Mobile Phones",
        "image": "https://res.cloudinary.com/demo/image/upload/categories/mobile-phones.jpg",
        "order": 1,
        "isActive": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  # Update Category

  ## Endpoint
  `PUT /api/categories/:id`

  ## Description

  Updates an existing category from the admin dashboard. Send only the fields you want to change. If `name` is changed, the backend regenerates the `slug`.

  ## Authentication

  Required. Admin only.

  Header:

  ```json
  {
    "Authorization": "Bearer <ADMIN_TOKEN>",
    "Content-Type": "application/json"
  }
  ```

  ## URL parameter

  `id`
  - Type: string
  - Required: yes
  - The category `_id`.

  ## Request body

  All fields are optional, but at least one field should be sent.

  ```json
  {
    "name": "Mobile Phones",
    "image": "https://res.cloudinary.com/demo/image/upload/categories/mobile-phones.jpg",
    "order": 1,
    "isActive": true
  }
  ```

  ## Field descriptions

  `name`
  - Type: string
  - Optional.
  - Changes the display name and regenerates the slug.

  `image`
  - Type: string
  - Optional.
  - Replaces the category image URL.

  `order`
  - Type: number
  - Optional.
  - Controls the position in `GET /api/categories`.

  `isActive`
  - Type: boolean
  - Optional.
  - `false` hides the category from public category listing.
  - `true` restores the category to public listing.

  ## Sorting effect

  After changing `order`, the updated category position changes in the public category list. The backend sorts by `order` ascending, then `createdAt` ascending.

  ## Sample success response

  ```json
  {
    "success": true,
    "message": "Category updated successfully",
    "category": {
      "_id": "662b7f8f4f9c2e2d7c111111",
      "name": "Mobile Phones",
      "slug": "mobile-phones",
      "image": "https://res.cloudinary.com/demo/image/upload/categories/mobile-phones.jpg",
      "order": 1,
      "isActive": true,
      "createdAt": "2026-04-20T10:00:00.000Z",
      "updatedAt": "2026-04-21T08:30:00.000Z"
    }
  }
  ```

  ## Common errors

  Category not found:

  ```json
  {
    "message": "Category not found"
  }
  ```

  Not logged in or not admin:

  ```json
  {
    "message": "Not authorized"
  }
  ```
