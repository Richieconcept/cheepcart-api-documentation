info:
  name: DELETE IMAGES TO CLOUDINARY
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseUrl}}/api/upload/delete"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  body:
    type: json
    data: |-
      {
        "public_ids": [
          "cheepcart/general/kbfy4oaberwh3yjwhl9s"
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  DELETE /api/upload/delete


  ## Description:
  Remove images from Cloudinary and keep the database in sync by deleting the public_ids.

  ## Authentication:
  - **Bearer Token** required.

  ### Body Type:
  - **Raw JSON**

  ## Request Body:
  ```json
  {
    "public_ids": ["cheepcart/products/new_abc123", "cheepcart/products/new_xyz456"]
  }
  ```
  ## Example response:
  ```json
  {
    "success": true,
    "message": "Images deleted successfully"
  }
  {
    "public_ids": ["cheepcart/products/new_abc123", "cheepcart/products/new_xyz456"]
  }
