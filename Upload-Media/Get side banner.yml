info:
  name: Get side banner
  type: http
  seq: 9

http:
  method: GET
  url: |
    GET {{baseUrl}}/api/side-banners
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
