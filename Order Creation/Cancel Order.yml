info:
  name: Cancel Order
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/api/orders/:orderId/cancel"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  params:
    - name: orderId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
