info:
  name: Retry shipment tracking admin
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/api/shipments/retry/:orderId"
  params:
    - name: orderId
      value: 69e355eb827eaadacc96e7ca
      type: path
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  # Retry Shipment Tracking Admin

  `POST /api/shipments/retry/:orderId`

  ## Description

  Forces an immediate Redstar tracking refresh for one order.

  Use this from the admin shipment monitor when a shipment already has a `trackingNumber` but automatic tracking failed, `shipmentRetryCount` increased, or `shipmentError` is present.

  This endpoint uses the order `_id`, not the tracking number.

  ## Authentication

  Required.

  ```http
  Authorization: Bearer <admin-token>
  ```

  Admin only. The backend route uses both `protect` and `adminOnly`.

  ## Path Parameters

  | Name | Type | Required | Description |
  |------|------|----------|-------------|
  | `orderId` | string | Yes | MongoDB `_id` of the order whose shipment tracking should be retried. |

  Frontend should use the `_id` from each row returned by `GET /api/shipments/get-all-shipments`:

  ```js
  POST /api/shipments/retry/${shipment._id}
  ```

  ## What It Does Internally

  - Finds the order by `orderId`.
  - Requires the order to already have a `trackingNumber`.
  - Calls Redstar tracking API.
  - Updates `deliveryStatus`, `orderStatus`, and `lastTrackedAt`.
  - Resets `shipmentRetryCount` to `0`.
  - Clears `shipmentError`.
  - If delivered, sets `orderStatus` and `shipmentStatus` to `delivered`.

  ## Example Request

  ```http
  POST /api/shipments/retry/69e355eb827eaadacc96e7ca
  Authorization: Bearer <admin-token>
  ```

  ## Success Response

  HTTP `200`

  The backend returns the full updated order document in `order`. Frontend can use these fields to refresh the shipment table row:

  ```json
  {
    "success": true,
    "message": "Shipment retried successfully",
    "order": {
      "_id": "69e355eb827eaadacc96e7ca",
      "orderNumber": "CHP-20260418-0001",
      "shipmentStatus": "created",
      "deliveryStatus": "in_transit",
      "orderStatus": "shipped",
      "trackingNumber": "RSASB1186443",
      "shipmentRetryCount": 0,
      "shipmentError": null,
      "lastTrackedAt": "2026-05-05T12:00:00.000Z"
    }
  }
  ```

  ## Tracking Not Available Yet

  If Redstar says the tracking number is not ready yet, the backend returns HTTP `200` so the admin UI can show a soft message instead of treating it as a hard failure.

  ```json
  {
    "success": true,
    "message": "Tracking not available yet. Try again later.",
    "shipment": {
      "status": "pending"
    }
  }
  ```

  In this response, no updated `order` is returned. Keep the existing table row and show the message.

  ## Shipment Not Found

  HTTP `404`

  ```json
  {
    "success": false,
    "message": "Shipment not found"
  }
  ```

  This means either the order does not exist or the order does not have a `trackingNumber` yet.

  ## Other Error Response

  HTTP `500`

  ```json
  {
    "success": false,
    "message": "Provider or server error message"
  }
  ```

  ## Frontend Notes

  - Show this action only to admins.
  - Use the shipment row `_id` as `orderId`.
  - Do not send the `trackingNumber` in the URL for retry.
  - Useful button label: `Retry tracking`.
  - Disable while request is running to avoid duplicate clicks.
  - After success, refresh the shipment table row with the returned `order`.
  - If the response has `shipment.status: "pending"` and no `order`, keep the row unchanged and show the returned message.
